---
title: "Why is my summary stuck on processing or failed?"
sidebarTitle: "Summary stuck"
description: "Summary jobs can be queued or fail temporarily; retry and check access/account state."
keywords: ["summary stuck", "processing", "failed", "retry"]
---

import { AiAgents } from '/snippets/ai-agents.mdx';

<AiAgents path="/answers/summary-stuck" />

If status is `queued` or `processing`, wait and refresh later.

If status is `failed`:

1. Click retry on the paper page.
2. Confirm you are signed into the project-owning account.
3. Try again after a short delay.

Most failures are transient and succeed on retry.
